# 2026-07-31 工作日志

## 权限矩阵回归测试补齐（M3 收尾）
- 重构 `cmd/server/main.go`：把路由构建抽成 `buildRouter(cfg, gdb, authSvc) http.Handler`（纯搬移，零行为变更），`main()` 仅初始化+调用+启 cron+监听。目的：让 handler 层测试能复用同一份路由定义。
- 新增 `cmd/server/permission_matrix_test.go`：用 `httptest` 跑真实路由，覆盖**全部 `RequireRole` 端点 × 5 角色（admin/sales/sales_lead/finance/hr）**，断言「禁角色→403 / 允许角色→非 403」，并抽查匿名→401。
- 关键模式：测试用 `middleware.GenerateToken(secret, emp)` 为**内存 Employee**（不落库、零副作用）签发 JWT；请求走真实 `buildRouter`，伪造 id 使 allowed 角色在权限层通过后落到 404/422（非 403），恰好验证 RBAC 而非业务。
- 矩阵表（cases）是路由权限的 single source of truth，改 `main.go` 的 `RequireRole` 角色或新增受保护端点须同步更新该表，否则测试报警。
- 校验：权限矩阵测试全绿；`go test ./...` 全绿；`gofmt` 干净。
- 注意：`go build ./cmd/server` 会在当前目录生成 `server` 二进制，提交前需 `rm -f server`，勿纳入版本库。

## 业务流 E2E 回归套件转正入库
- 将 `/tmp` 下 8 个 E2E 脚本（审批/合同/开票/回款/提醒/公海/报表/离职）扶正到 `tests/e2e/`，并建立统一入口 `tests/e2e/run_all.sh` + Makefile `e2e` 目标（构建临时 server+seed → 顺序跑 8 脚本 → 汇总 → 清理），成为受控可重复运行的回归套件。
- 统一脚本约定：5 个 Convention B 脚本（approvals/contracts/invoices/payments/reminders）由硬编码 `BASE=".../api/v1"` 改为 `sys.argv[1]` 可配 + 派生 `API`，与 3 个 Convention A 脚本一致；m24 默认端口 8081→8080。
- 修复共享库场景下 3 类脆弱断言：
  1. `e2e_reminders` dashboard 三条绝对值断言改为「基线 + 本脚本增量」（seed + 其他脚本会累加金额，绝对值必然偏差）；
  2. `e2e_m24` 的「交接前无 offboard 审计=0」改为「针对 demo-sales 的 offboard 记录=0」的精确过滤（隔离 m31 离职退公海的审计噪音）；并把员工 id 解析提前避免 NameError；修正「缺失交接人→422」为「无业务→退回公海200 / 有商单→422」的真实业务语义（补建有商单员工的守卫用例）；
  3. `e2e_contracts/e2e_payments/e2e_reminders` 的「商单→won」「合同→signed」改为经**审批流**（状态机要求 won/signed 必须经审批，直接推进被拒）。
- 顺序编排：`reports` 必须在 `m24` 之前（m24 会停用 demo-sales）；`m31` 的 offboard 用自建员工，已被 m24 过滤隔离。
- 校验：`make e2e` 全套 8 脚本 ALL PASS（approvals 28 / contracts / invoices 14 / payments 26 / reminders 28 / m31 44 / reports 34 / m24 37）。

## 关键踩坑（E2E 转正）
- 商单状态机：won 只能从 pending_approval 经审批服务（ApplyDiscountAndWin）推进，普通 ChangeStatus 不允许 negotiating→won；合同 signed 同理须经 contract_sign 审批。E2E 脚本必须走审批流才能到达终态。
- dashboard 全局聚合断言必须用「基线+增量」隔离跨脚本副作用；审计查询断言须按 entity_id 精确过滤。
- demo-sales 由 seed 保证存在（Bss@1234，role=sales，MustChangePwd=false），runner 先 seed 再跑套件是其依赖前提。

## 收尾清理
- 提交 `cfedc17` 误将 `tests/e2e/__pycache__/*.pyc` 纳入版本库。已补 `tests/e2e/.gitignore`（`__pycache__/` + `*.pyc`），`git rm --cached` 取消跟踪并本地删除缓存，提交 `b311181` 推送。工作树已干净。

## M3-2 Excel 数据导入（2026-07-31）
- 新增依赖 `github.com/xuri/excelize/v2` v2.11.0（goproxy.cn 可用），用于解析/生成 .xlsx。
- 后端：`internal/services/import.go`（解析首个工作表「客户导入」，按列名映射，逐行事务创建客户+可选联系人，返回摘要含失败行号）、`internal/handlers/import.go`、路由 `POST /api/v1/imports/customers` 与 `GET /api/v1/imports/customers/template`（限 admin/sales/sales_lead）。复用 customers/contacts 表，**无新迁移**。
- 前端：`web/src/pages/ImportCustomers.tsx`（拖拽上传 + 模板下载 + 结果统计/错误明细），菜单「Excel 导入」。
- 测试：`import_test.go`（基础/重名跳过/无效负责人邮箱）、`cmd/server/m3_e2e_test.go`（httptest 复用生产路由跑通 模板下载→上传落库→项目/成员/任务 全链路）。

## M3-3 项目/交付管理（2026-07-31）
- 迁移 `0008_projects.sql`：projects / project_members（计划/实际人天，REAL）/ project_tasks（任务+里程碑合并 kind）。**关键坑**：新表 `created_at/updated_at/deleted_at` 必须声明 `DATETIME`（不是 TEXT），否则 glebarez 按 string 返回、Scan 到 `time.Time` 报错（0008 初稿误用 TEXT，已改）。
- 模型：Project 增加 `Members []ProjectMember` / `Tasks []ProjectTask` 关联字段（Get 用 Preload，缺字段会报 `unsupported relations`）。
- `ScopeProject`：admin/finance/hr 看全部；销售/主管看「自己负责或自己是成员」；写操作鉴权 owner/成员。人天非金额，允许 REAL 小数。
- 前端：`web/src/pages/Projects.tsx`（列表 + 新建/编辑 + Drawer 三 Tab：任务/里程碑/成员含人天汇总）。
- 验收：`go test ./...`、`pnpm build`、`make e2e`（ALL PASS 无回归）全部通过。

## 提交计划
- 待提交：M3-2 + M3-3 全部代码、迁移、测试、文档（DEV_PLAN M3-2/M3-3）、前端构建产物（web/dist 由 embed 承载，dist 本身 gitignore？检查）。
