# 2026-07-30

## 修复：AntD `useForm` 未连接 Form 警告
- 现象：`Warning: Instance created by useForm is not connected to any Form element. Forget to pass form prop?`
- 根因：4 个含 `<Form form={form}>` 的 Modal 都设了 `destroyOnClose`；弹窗关闭时 `<Form>` 被卸载，但 `openEdit` 中的 `form.setFieldsValue(...)` 与新规「新建」按钮的 `form.resetFields()` 在弹窗仍关闭（Form 未挂载）时调用，触发警告。
- 修复：移除 Deals/Employees/Customers/CustomerDetail 中“表单类”Modal 的 `destroyOnClose`（表单已在 onCancel/关闭 时 `form.resetFields()`，无需销毁重建）。流转/输单/转移 3 个纯 Select Modal 无 useForm，保留 destroyOnClose 无影响。
- 教训：AntD 中 `Form.useForm()` + `destroyOnClose` 同用时，绝不可在弹窗关闭态调用 form 方法；要么去掉 destroyOnClose，要么改用 `forceRender` 保持 Form 挂载。
- 已 `pnpm run build` 通过（web/dist 已更新）。

## 补充测试（用户要求“完整补充测试”）
- 根因复盘：此前 bug 多未被发现，是因**前端零测试** + **后端测试只走 happy path、且缺 error-handling lint**。
- 前端（此前完全空白）：
  - 引入 Vitest 2 + @testing-library/react 16 + jest-dom + user-event + jsdom（`web/package.json` 加 devDeps 与 `test`/`test:watch` 脚本；`vite.config.ts` 加 `test` 段；`web/src/setupTests.ts` 补 matchMedia/ResizeObserver 等 jsdom polyfill）。
  - 共享件：`web/src/test/render.tsx`（QueryClientProvider+MemoryRouter+AntD App 包装）+ `mockApi.ts`（`buildApiMock(importOriginal, overrides)` 部分 mock `../api`）。
  - 新增 4 个页面组件测试（Deals/Customers/Employees/CustomerDetail），共 **14 用例**，专门锁死已修复的崩溃场景：打开新建/编辑弹窗后 `<Form>` 已连接、断言**不出现** “not connected to any Form element” 警告；closed(null) 不抛错；编辑回填字段。
  - 结论：**实测证明移除 `destroyOnClose` 确实消除了该警告**（警告断言全过）。
- 后端：
  - 新增 `internal/services/employee_test.go`（Create/Update/SetStatus/ResetPassword 守卫 + 字典去重/占用校验），补齐此前零覆盖的 employee 服务。
  - 新增 `.golangci.yml`（启用 errcheck/errorlint/staticcheck 等，重点拦“吞 error”）与 `Makefile` 的 `lint`/`test-web`/`test-all` 目标。注：本机装不上 golangci-lint（go install 被 kill），配置与 target 供 CI 使用；`go vet ./...` 已验证干净。
- 校验：`go test ./...` 全绿；`pnpm test` 14/14 通过；`tsc -b` 与 `go vet` 均无错。
- 提交：`9ab4677`（13 文件，+1350）。

## M1 Sprint 4 合同 + 附件 —— 完成并推送
- 提交 `32ed7cd`（push 到 origin/main，9ab4677..32ed7cd）。
- 内容：contract service（状态机 / N:N 同客户 won 校验 / 终态锁定 editableFields / 附件白名单 20MB）、handler+路由（HT- 单号、RBAC ScopeOwner、下载走鉴权接口）、resp 补 3005/3006、前端 Contracts 页（列表/新建编辑/状态流转/附件面板）+ 路由菜单、后端 8 项 service 单测 + 前端 4 项组件测试、DEV_PLAN 标记 ✅。
- 校验全绿：`go vet` 干净；`go test ./...` 全过；`pnpm run build` 通过；`pnpm test` 18/18。

## E2E 验证脚本踩坑（可复用）
- 端到端脚本 `/tmp/e2e_contracts.py`（curl 测登录 200 但脚本报 `KeyError: 'token'`）。
- 根因：本系统 API **所有成功响应都包在 `{"code":0,"data":{...}}` 信封里**（resp.Body 的 `Data` 字段），但脚本误以为 `token`/`id` 在顶层。修复：让 `req()` 在成功 JSON 时返回 `j.get("data", j)`，所有资源访问（c1["id"]、ct1.get("code")、att.get("id")、body["token"]）随之成立。
- 教训：写调用方断言时务必先看 `resp.OK` 的包裹结构，别假定顶层字段。
- 另一坑：本机 `HTTP_PROXY=http://127.0.0.1:6478` 且 `NO_PROXY` 含 `127.0.0.1`，urllib 偶发走代理导致本地请求异常；E2E 启动 server 前务必 `pkill -f <bin>` 清孤儿进程 + 干净 `BSS_DATA` 目录，避免 SQLite 锁/连错实例。
- **E2E `req()` 入参顺序坑（新增记录）**：`req(method, path, body=None, token=None)`。DELETE/GET 带鉴权务必写成 `req("DELETE", path, token=token)`（关键字传 token），若写成 `req("DELETE", path, token)` 会把 token 当 body、实际 `token=None` → 收到 **401**。本次 Sprint 5 E2E 因此误报 401，curl 复现证明后端 DELETE 已核销计划返回正确 422。

## M1 Sprint 5 回款 —— 完成并推送
- 提交 `783c4d4`（push 到 origin/main，32ed7cd..783c4d4）。
- 内容：payment service（计划总额≤合同额、已核销 pending 以外禁改禁删、记录核销推进 pending→partial→paid、合同维度汇总 应收/已收/余额/逾期额、is_overdue 派生、跨合同核销拒绝）、handler+路由（计划 CRUD 排除财务 RequireRole admin/sales/sales_lead；回款录入/删除仅 admin/finance；ScopeOwner 行级校验；错误映射 3001/3002→422）、前端 Payments 页 + PaymentCenter 抽屉（汇总卡片/计划表/记录表/录入表单，按角色门控 `me.role`）、后端 6 项 service 单测 + 前端 3 项组件测试、DEV_PLAN 标记 ✅。
- 校验全绿：`go vet` 干净；`go test ./...` 全过；`pnpm run build` 通过；`pnpm test` 21/21；E2E `/tmp/e2e_payments.py` 26 断言全绿（计划上限 422、已核销锁定 422、跨合同 422、销售录入 403 / 财务 200、汇总准确到分）。
- 业务约定：回款记录录入采用 **admin/finance** 双角色（财务为主、admin 超管兜底）；计划管理采用 **admin/sales/sales_lead**（排除财务，对齐 PRD §6）。

## M1 Sprint 6 提醒+仪表盘+联调 —— 完成并推送（M1 出口）
- 提交 `174e407`（push 到 origin/main，783c4d4..174e407）。M1 全部 Sprint 完成。
- 内容：notification service（每日扫描合同30天内到期+回款逾期→notifications，dedup_key=type|entity_id|关联日期 去重；列表/未读/单条+全部已读，归属校验 ErrNotificationForbidden；仪表盘 4 卡片+3 列表 全 ScopeOwner 过滤，计划类查询走 contract_id 子查询）、handler+路由（/notifications、/dashboard、admin/scan-reminders 手动触发）、internal/cron 每日09:00 触发、前端通知中心铃铛+未读角标+下拉列表+标记已读（MainLayout）、仪表盘页 4卡3表、造数脚本 cmd/seed、备份脚本 scripts/backup.sh。
- 校验全绿：`go vet` 干净；`go test ./...` 全过（新增 4 项 notification 单测）；`pnpm run build` 通过；`pnpm test` 25/25（新增 Dashboard + NotificationBell 2 项组件测试）；E2E `/tmp/e2e_reminders.py` 29 断言全绿（扫描生成2条、去重、通知列表/未读、仪表盘聚合到分、标记已读/全部已读）。
- 踩坑：payment_records/deals 无 owner_id 列，ScopeOwner 不能用 `.Table("payment_records").Scopes(ScopeOwner)`（会加 owner_id 过滤报列不存在）；仪表盘卡片2（本月回款）改用 `contract_id IN (allowedContractIDs子查询)` 过滤。
- 下一步：进入 M2（审批流/开票/报表中心/审计查询页），按 PRD 附录 A 排期。

## M2 二期增强 —— 全部完成并推送
- M2-1 审批流 `184040c` / M2-2 开票 `b554254` / M2-3 报表中心 `b3cdac7` / M2-4 审计查询+离职交接 `e616ecf`（均 push 到 origin/main）。
- M2-4 内容：
  - 后端：`audit_query.go`（audit_logs 按 entity_type/entity_id/action/operator_id/时间范围过滤 + 分页，仅 admin/hr/finance）；`employee.go` 的 `OffboardPreview`/`Offboard`（事务内批量转移客户/商单/合同的 owner_id 给交接人 + 停用 + 手写 offboard 审计；守卫：非本人/交接人存在且 active/非最后 admin/非本人操作）；handlers `audit.go` + `employee.go` 两个 offboard handler；main.go 注册 3 条路由。
  - 前端：审计查询页 `Audit.tsx`（筛选+表格+JSON 详情，菜单按角色条件渲染）；员工离职交接弹窗（停用前预览数据量 → 选交接人 → 确认转移停用）。
  - **修复真实 bug**：`Offboard` handler 的 `successor_id` JSON tag 原为 `uint`，但前端以字符串传递 → 400；改为 `json:"successor_id,string"`（与 `customer.Transfer` 的 `owner_id,string` 一致）。
  - 测试与校验全绿：`go test ./...` 通过；`pnpm test` **34/34**（含 Employees/Audit 组件测试，修正 hoist 陷阱改用桩 mock + `getByRole` 取真实 button 判 disabled）；`pnpm run build` 通过；E2E `/tmp/e2e_m24.py` **33 断言**全绿（审计权限/过滤/分页 + 离职预览/转移/停用/审计落地 + 4 项守卫）。
- docs/DEV_PLAN.md 的 M2 四 Sprint 全部标记 ✅，注明 commit 与验收状态。
- 结论：**M2 已收尾，M1+M2 全链路可演示**。后续可选进入 M3（PRD 附录 A Backlog：项目交付/企微通知/公海池/Excel 导入）。

## M3-1 客户公海池 —— 完成并推送
- 提交：feat: M3-1 客户公海池（push 到 origin/main）。
- 内容：无主客户池（`owner_id=0`）+ 领取/释放/指派/流水/回收规则 + 离职退公海打通。
  - 迁移 `0006_customer_pool.sql`（customers 加 last_followed_at/claimed_at/pool_reason；customer_pool_logs 流水；pool_settings 单行规则）+ `0007_drop_customers_owner_fk.sql`。
  - 后端：`internal/services/pool.go`（PoolService：Claim/Release/Assign/Recycle/List/Logs/Settings，含领取上限、有商单/合同禁止释放、超期回收含保护期豁免、TouchFollow 刷新跟进时间）、handlers/pool.go、main.go 路由（公海全员可读；领取/释放 admin/sales/sales_lead；指派/回收 admin/sales_lead；规则设置仅 admin）、internal/cron 每日 02:00 自动回收。
  - 离职打通：`employee.go` 的 `Offboard(successorID=0)` 把客户退回公海并写 pool_log；仍有商单/合同时 `ErrSuccessorRequired` 不允许仅退回。
  - 前端：`web/src/pages/CustomerPool.tsx`（列表/领取/指派/流水/回收规则设置/试算回收）、`Customers.tsx` 加「释放」入口、App.tsx + MainLayout 加「公海池」菜单、api/index.ts 增 8 个接口与类型、CustomerPool.test.tsx 组件测试。
  - 测试：后端 pool_test.go 9 项单测；前端 CustomerPool.test.tsx 5 项；E2E `/tmp/e2e_m31.py` **44 断言全绿**。
- **关键踩坑（重要，勿再犯）**：`customers.owner_id` 在 `0001_init.sql` 带 `REFERENCES employees(id)` 外键，置 0（公海）会触 `FOREIGN KEY constraint failed`。修复用 `0007` 临时表换名法去掉该外键——**注意**：若直接 `ALTER customers RENAME TO customers_old` 再建新表，SQLite 会自动把 `contacts/deals/contracts` 的子表外键重写指向 `customers_old`，DROP 后子表外键悬空导致 "no such table: customers_old"。正确做法：新建 `customers2`（不含 owner 外键）→ 复制 → `DROP customers` → `ALTER customers2 RENAME TO customers`，子表外键按名字 "customers" 自然解析到新表，无需重建子表（DROP 原表需 `PRAGMA foreign_keys=OFF`，故 `0007` 用 `-- +goose NO TRANSACTION`）。
- 交付形态结论（M3 询问时确定）：本期不做交付管理，但若未来做，按 **projects + milestones + tasks** 三层（参考 SF 的 Awaiting Start/In Progress/Awaiting Signature/Completed 阶段），人天挂在 task 上。
- 校验全绿：`go test ./...` 全过；`pnpm test` **39/39**；`pnpm run build` 通过。
